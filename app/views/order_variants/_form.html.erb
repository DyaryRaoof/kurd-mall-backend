<%= form_with(model: order_variant, url: url) do |form| %>
  <% if order_variant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_variant.errors.count, "error") %> prohibited this order_variant from being saved:</h2>

      <ul>
        <% order_variant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id, class: "form-control" %>
  </div>

  <div>
    <%= form.label :item_variant_id, style: "display: block" %>
    <%= form.text_field :item_variant_id, class: "form-control" %>
  </div>

  <div>
    <%= form.submit "Submit", class: "btn btn-primary mt-5"  %>
  </div>
<% end %>
