<%= form_with(model: city) do |form| %>
  <% if city.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
        <% city.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name_en, style: "display: block" %>
    <%= form.text_field :name_en, class: "form-control" %>
  </div>

  <div>
    <%= form.label :name_ku, style: "display: block" %>
    <%= form.text_field :name_ku, class: "form-control" %>
  </div>

  <div>
    <%= form.submit "Submit", class: "btn btn-primary mt-5" %>
  </div>
<% end %>
