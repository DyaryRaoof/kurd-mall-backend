<%= form_with(model: item_star, url: url) do |form| %>
  <% if item_star.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(item_star.errors.count, "error") %> prohibited this item_star from being saved:</h2>

      <ul>
        <% item_star.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.text_field :item_id, value: params[:item_id], class: "form-control" %>
  </div>

  <div>
    <%= form.label :number, style: "display: block" %>
    <%= form.number_field :number, class: "form-control" %>
  </div>

  <div>
    <%= form.label :reviewers, style: "display: block" %>
    <%= form.number_field :reviewers, class: "form-control" %>
  </div>

  <div>
    <%= form.submit 'Submit', class: "btn btn-primary mt-5" %>
  </div>
<% end %>
